# Cordon — Python dependencies
#
# Nothing in this file is needed to RUN Cordon. If you are here to get the
# extension working, you want:
#
#     npm run setup
#
# Cordon is a TypeScript/Node project; everything it actually needs is in
# package.json. Python is used only to rebuild the presentation artefacts.
#
# Python is used only for the presentation toolchain:
#   python -m pip install -r requirements.txt
#   python docs/build_deck.py        → docs/Cordon_SIH26171.pptx
#
# Tested on Python 3.11.9.

python-pptx==1.0.2

# Pulled in by python-pptx; pinned so a fresh machine reproduces the deck byte-
# for-byte rather than picking up a newer transitive release.
lxml==6.1.2
pillow==12.2.0
XlsxWriter==3.2.9
typing_extensions==4.15.0
