node_modules/
dist/
*.log
.env
.env.*
!.env.example
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store

# The ViT classifier is 84 MB — fetched with `npm run fetch-models`, not committed.
# UltraFace (1.2 MB) IS committed, because the extension needs it to work at all.
extension/public/models/vit-base-patch16-224/

# Written by `npm run eval`
eval_output.json

# Chrome for Testing, fetched by @puppeteer/browsers for the browser check
chrome/

# Written by `npm run browser-check`
eval/screenshots/
